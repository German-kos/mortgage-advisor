export type ContactBlock = {
  img: string;
  title: string;
  content: string;
};

export type FooterLink = {
  name: string;
  link: string;
};

export type SocialLink = {
  name: string;
  link: string;
};
