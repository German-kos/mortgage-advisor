export type FormProps = {
  showSubtext?: boolean;
};

export type FormData = {
  name: string;
  email: string;
  phone: string;
  time: string;
};

export type FieldConfig = {
  name: string;
  label: string;
  type: string;
};
